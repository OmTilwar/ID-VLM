# ID-VLM — Dependencies
# Core (local development: data processing, evaluation, tests)
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.8.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
python-Levenshtein>=0.21.0
tqdm>=4.65.0

# API (stretch goal)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Testing
pytest>=7.4.0
httpx>=0.25.0

# ──────────────────────────────────────────
# GPU / Colab-only dependencies (install in notebook, not locally):
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install --no-deps trl peft accelerate bitsandbytes
#   pip install qwen-vl-utils
# ──────────────────────────────────────────
